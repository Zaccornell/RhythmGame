using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GreenNoteSpawn : MonoBehaviour
{

    public GameObject greenNotePrefab;
    public Vector3[] greenNoteSpawnPoint;

    // Start is called before the first frame update
    void Start()
    {
        //Instantiate(greenNotePrefab, greenNoteSpawnPoint, Quaternion.identity);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
